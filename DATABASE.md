# Database Direction

V1 target: PostgreSQL with migrations and typed access layer.

The repository scaffold does not connect to a live database yet. T001 only establishes boundaries. The first persistence task must select and implement the ORM/migration tooling through an ADR.
